let guest = ['Shahzaib', 'Honey' , 'Shah']

for(let i=0; i<guest.length; i++){

    console.log(`Dear ${guest[i]}, you are cordially invited to dinner at my place on Saturday. `);
}

// for each

// Define the guest list
const guestList = ['Fiza', 'Zaynab', 'Mirha'];

// Iterate over the guest list and print an invitation message to each guest
guestList.forEach(guest => {
  console.log(`Dear ${guest}, you are cordially invited to dinner at my place on Saturday. `);

});