let usernames = ["Alice", "Bob", "Charlie"];
if (usernames.length === 0) {
    console.log("We need to find some users!");
}
else {
    usernames = []; // remove all usernames from the array
    console.log("All usernames have been removed.");
}
export {};
